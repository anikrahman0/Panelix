<div class="card sticky-pub">
    <div class="card-header">
        <h5 class="card-title">{{$cardLabel}}</h5>
    </div>
    <div class="card-body">
        <button type="submit" class="btn btn-primary mb-2">{{$submitLabel}}</button>
        <a href="" class="btn btn-danger mb-2">{{$discardLabel}}</a>
    </div>
</div>